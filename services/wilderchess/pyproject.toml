[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wilderchess"
version = "0.1.0"
description = "ML-driven strategy engine"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "pandas",
    "scikit-learn",
    "joblib",
    "seaborn",
    "matplotlib",   # For the model execution we discussed earlier
    "onnxruntime",
    "onnx>=1.21.0",
    "onnxscript>=0.7.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/wilderchess"]
